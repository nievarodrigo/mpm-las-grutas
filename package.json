{
  "name": "mpm-las-grutas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css script.js robots.txt sitemap.xml dist/ && cp -R public dist/public"
  }
}
